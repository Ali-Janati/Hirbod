# توکن‌های اولیه هیربد — سرور 192.168.100.25

> این توکن‌ها در `sql/schema.sql` قرار دارند. پس از نصب، آن‌ها را در جای امن نگه دارید.
> مدیر می‌تواند از داخل پنل، توکن جدید بسازد.

| نقش | نام | توکن |
|-----|-----|------|
| **مدیر (admin)** | مدیر اصلی | `hrb_a7f3c9e2b1d84f6a0e5c8b3d7f2a941` |
| **ناظر (viewer)** | ناظر | `hrb_4e8b2a1c9d7f3e6b0a5c8d2f7e9413b` |
| **کاربر (user)** | مشتری اول | `hrb_9c1d5f8a2b7e4c3d6f0a8b5e2d71394` |
| **کاربر (user)** | مشتری دوم | `hrb_2f7a9c4e8b1d5a3f6c0e9b7d4a82156` |
| **کاربر (user)** | مشتری سوم | `hrb_6b3e8d1f5a9c2b7e4f0d6a3c8b51479` |

## آدرس‌ها

- **اپ:** http://192.168.100.25/hirbad/
- **API:** http://192.168.100.25/hirbad/api/

## نصب سریع روی لینوکس

```bash
# کپی پروژه به سرور
scp -r Hirbod/ user@192.168.100.25:/tmp/hirbad

# روی سرور
cd /tmp/hirbad
chmod +x deploy/install.sh
sudo ./deploy/install.sh
```

## ساخت توکن جدید (از پنل مدیر)

1. با توکن مدیر وارد شوید
2. بخش «مدیریت توکن‌ها» → نام و نقش را وارد کنید → «ساخت توکن جدید»
3. توکن ساخته‌شده را یک‌بار نمایش می‌دهد — آن را کپی و ذخیره کنید
